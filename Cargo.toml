[package]
name = "f3m"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
primal = "0.3"
num-traits = "0.2" # Zero, One traits used by the matrix module

[dev-dependencies]
proptest = "1"     # property-based tests for matrix algebra laws
