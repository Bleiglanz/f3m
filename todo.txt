TODO

(items 1–73 archived in history/todo_1_73.txt)

TODO 74) [DONE]
add a new checkbox "Strata" (next to S,Kunz,Gaps,Class)
it is not checked at the beginning
theory of strata (with left={1,...\mu-1} and right={mu+1,..m-1}
for l = 0...level(S) - we have defined level as floor(f/m)
define M_l := lm + i for i in left
define N_l := lm + i for i in right
then M_l - lm is a subset of left
and  N_l - lm is a subset of right
the idea would be that if strata is checked we see
a thick black border around the sets M_l and N_l, i.e. on every level we
would see
   |------------|        |------------|
lm |            | mu+lm  |            |
   |------------|        |------------|
so for every element in the strata we need to check
which borders to draw, something linke
i=1 only left, bottom, top border
1<i<mu-1 only top and bottom border
mu-1 only right and top and bottom
and then the same for i in mu+1...m-1

TODO 75) [DONE] I have not given up on the idea of counting semigroups
before going back to todo 73-72-71 let's study the general case
create a second wasm-html file strata.html
reuse as much code as possible, css, js etc.
Name should be "strata exporerer"
here we abstract from the left-right as in todo 74, lets go general:

input is a usize N and a lmax:usize

a strata sequence for l=0...l_max is a sequence of subsets
M_0 = emptyset contained in M_1 contained in M_2 .... contained in M_lmax contained in {1,...,N}
the only condition is that they go up,

should be in a html table like in index html
columns are 1,...,N
rows are l=0,...,lmax (lmax on top, next one down is lmax - 1, down to 0)

One Button Rnd1 : it creates a random subset at level 1 (one above the bottom of the table)
and fills the sequence by random numbers
at each step add something or do nothing - sets can only get bigger

the <th> from 1...N is repeated at the bottom

TODO 76) [DONE] currently the col index i is in the cells - replace by the number lm+i

TODO 77) [DONE] there is a simple symmetry 1<->N, 2<->N-1, 3<->N-2
col i corrosponds to col N-i+1
this would be if our invisible frobenius would be just lmax + N + 1--but we don't have that here
if mouse over col i, highlight both columns i and N-i+1

TODO 78) [DONE] if in each cell in col i is the number lm+i, find
the minimum number w_i in each cell (it is the first time it appears while l goes up)
add a new row at the bottom
w_1,...w_N
and another one showing these minimus

TODO 79) [DONE] show missing - call the value val(l,i)=lm+i
the cell l,i represents the number ml+i
if i hover a empty cell: highlight the gaps (= empty cells)
that have the val(l,i)+w_j for all j

TODO 80) [DONE] in the 3d view only in the tab
add two buttons "run descent" and "run ascent"
clicking on them will toggle the description to "stop descent" resp. "stop ascent"
if one is running, user cannot click on the other one
"run ascent" is only available if is_descent is true, i.e. we can ascent at all
once running, it does at most 500 iterations of
compute the new semigroup descent or ascent of the current one (but don't change the view)
do an animation
update the 3dview with the new semigroup, but keep all viewport settings as the user has it
wait for 1second
the animation is this

descent of an apery w such that f < w < f+m
--animates the movement of the cube w to the w-m position
--lets the gap-cube w-m directly below it explode
--lower the cube to the w-m position one level down
--replace the semigroup with the new one
descent of f+m
--let f explode
--lower the f+m cube to the f position
--replace the semigroup with the new one
ascent of a minimal generator a with f-m<a<f
--let the a cube explode
--replace the view with the new semigroup (colors could change)
ascent of a minimal generator f+m
--let the f+m cube explode
--replace


TODO 81) [DONE]
directly below the random button show a small text
constraint = 0
and a textbox below (same width as the buttons)
in the box the user can enter a expr
exactly the same capabilites as our expr parser allows
if there is anything in this box:
if the user clicks on random, the constraint is given
to the create random as a string
random goes into its loop
creates a random semigroup
computes the value of the constraint expr with e,m,t,r,... etc
(if the expr throws an error - ignore)
if value of the expr == 0 then return the semigroup
otherwise iterate
(ignroe the problem that the expr==0 might be impossible - too hard to adapt the random parameters we use now)

TODO 82) [DONE]
added new props in semigroup.rs
    /// es = number of small minimal generators, the ones <f-m
    pub es: usize,

    /// rl = large reflected gaps, gaps L such that f-m < L < f (automatically reflected)
    pub rl: usize,

add them to the history, short_props, waldicone summary+json, Pivot
also reorder the history, shortprops
instead of
m	f	e	g	σ	r	ra	fg	t
use
m   f  es   e   σ   g  rl   t  r   ra  fg
in integration.rs check the tests
    // todo 82
    assert!(s.rl <= s.t);
    assert!(s.t <= s.r +1);
    assert!(s.es <= s.e);


TODO 83) [DONE]
combined_table now emits a top "sg-arrow-row" with one cell per residue
column:
- residue 0: ↓ → toggle(m)
- residue i ≠ 0 with w_i > f: ↓ → toggle(w_i)
- residue i ≠ 0 with f − m < w_i < f: ↑ → toggle(w_i)
- otherwise: empty
The arrow spans carry `data-n` so the existing `.sg-grid span[data-n]`
click handler routes the click through doToggle.
Visual check on ⟨6, 9, 20⟩: res 0 ↓6, res 1 ↓49, res 2 empty, res 3 empty,
res 4 ↑40, res 5 empty.

TODO 84) [DONE]
PROP_THEAD_TR adds a final `<th>Δt</th>` column; historyRow appends a
`<td>` showing `t(current) − t(previous)` (signed, "—" for index 0).
CSV header gets a matching `Δt` column. Help-tab description updated.
The Comp-tab shortprop (Rust) is unchanged because it always shows
prev/curr side-by-side.

TODO 85) [DONE]
  - Almost-symmetric invariant: already asserted at integration.rs:486
    (`assert_eq!(s.ra, s.r, "almost-symmetric ⇒ ra=r for {gens:?}")`).
    Audit item was a false alarm — no change needed.
  - rl ≤ t strictness: strengthened to `rl < t` whenever `t > 0`. Proof
    in the comment at check(): every gap counted by rl is pseudo-Frobenius
    and strictly below f, while f itself is always in PF and never in rl,
    so rl + 1 ≤ t.
  - Pivot tab drill-down field-shelf polish: deferred — vague item, no
    concrete pain point identified.
  - Waldicone HTML sort/filter: deferred — adding sort interactivity
    needs a JS lib (tablesort.js or similar). Users wanting sortable
    views have the JSON + Pivot tab.

TODO 86) [DONE] up-down = identity?
findings (commit 4e175f9 + the experiment in this commit):
- descent(ascent(S)) == S holds iff ascent enters its branch (2):
      f+m is a min-gen AND no min-gen g satisfies m < g < f and g+m > f
  the "large min-gen" branch (1) is NOT invertible by descent in general.
  proof sketch in the manipulators::ascent doc comment.
- counterexample: ⟨4, 5, 7⟩ -> ⟨4,7,9,10⟩ -> ⟨3, 4⟩ (test
  test_up_down_identity_fails_in_large_gen_branch in integration.rs).
- the gated identity is asserted in the main check() loop and as a
  dedicated test test_up_down_identity_fpm_branch in integration.rs.
- duality between Semigroup::ascent and Semigroup::descent is documented
  in their doc comments in manipulators.rs.

TODO 87) [DONE]
test_up_downs(s) reviewed and documented. Each branch is justified in the
doc comment on the function; the conditions are:
  - descent: w > f && f > m  (always: count_gap+1; legs gated by
    v_in_s vs w<f+m)
  - ascent : w ∈ gen_set && (w<f && w+m>f  OR  w == f+m)
All assertions hold across the full CSV fixture set + property tests
(integration suite: clean run, no failures).
Outer descent guard `f > m` skips f≤m (e.g. ⟨2,3⟩); descent `w == f+m`
with `!v_in_s()` only asserts `count_gap+1` (other invariants unconstrained
for that geometry).

TODO 88) [DONE]
arrow_cell in html_helpers/src/combined_table.rs now mirrors test_up_downs:
  - residue 0: never an arrow (test loop is 1..m)
  - ↓ iff `w > f && f > m`, data-n = w − m (the gap that descent adds)
  - ↑ iff `w ∈ gen_set` and either `f − m < w < f` or `w == f + m`
CSS: font-size 1.6em, font-weight 900, row height bumped to 2em.

TODO 89) [DONE]
Three buttons added in index.html under the existing Sym/PSym/ASym row:
  "Sym (r=0)+f", "PSym (r=1)+f", "ASym (r=t-1)+f".
driveCreator / driveRandomCreator now accept an optional postProcess(sg);
the +f buttons pass `addF = sg => sg.toggle(sg.f).gen_set`. Constraint
matching is applied to the *base* semigroup, before toggle(f). Help-tab
row updated.

TODO 90) [DONE]
tests/data/check_fixtures.csv holds 108 semigroup fixtures, format:
  gens;e;f;m;g;c;sym;apery;pf;t   (inner arrays space-separated).
One test (test_csv_fixtures) parses each row and calls check(); all
108 wrapper fns removed from integration.rs (file shrank ~2k lines).
On failure, the gens-bearing assertion messages in check() still
identify the offending row.

TODO 91) [DONE]
Removed `max_ri()` and `any_ri_eq_2()` from `semigroup.rs`.
Cones (`semigroup_cones/src/main.rs`): dropped the `∃r_i=2` summary
column (totals shrank from 15 to 14), the by_max_ri distribution
table, the `max r_i` / `min r_i` per-row HTML columns (replaced by a
single `ρ` column), and the `max_ri`/`any_ri_eq_2` JSON fields.
Tests: replaced `test_min_max_any_ri` with `test_rho`. JS pivot row
projection drops `min_ri/max_ri/any_ri_eq_2` and gains `rho`.

TODO 92) [DONE]
Renamed `Semigroup::min_ri()` → `Semigroup::rho()` everywhere.
Touched: `semigroup_math/src/math/semigroup.rs`,
`semigroup_math/tests/integration.rs` (incl. doc comment and the
`minr` locals in `test_up_downs`), `semigroup_cones/src/main.rs`,
`jsmodules/app.js` (pivot projection), `tex/writeup_updown.tex`
(notation table + the verbatim code block).

TODO 93) [DONE]
Audit pass:
  - `README.md` properties table: added `ρ` and `Deep` rows so it
    matches the help-tab's Key Properties section.
  - `index.html` help-tab: "Kunz coefficients" entry now describes
    `r_i` and `ρ` (was `r_i / min r_i / max r_i`). Pivot tab field
    hint updated.
  - `index.html` (help-tab History row) and `jsmodules/app.js`
    (PROP_THEAD_TR + CSV header) were already consistent after the
    earlier Δt removal.
  - Cached HTML reports under `normaliz/` and `history/` still
    contain the old `min r_i / max r_i / ∃r_i=2` headers; those are
    waldicone output and will refresh on the next CLI run, so they
    are intentionally not edited here.

TODO 94) [DONE]
Renamed `Semigroup::count_gap` → `g` and `Semigroup::count_set` → `sigma`
across the workspace (~73 sites). Touched:
  - `semigroup_math/src/math/semigroup/mod.rs` (struct fields + docs) and
    the `compute()` builder in `semigroup_compute.rs` (locals + init).
  - `semigroup_math/src/math/mod.rs` (GAP block emission).
  - WASM getters in `semigroup_explorer/src/lib.rs`: `count_set` → `sigma`,
    `count_gap` → `g`. JS reads `s.g` / `s.sigma`; delta-info DELTA_PROPS
    keys updated.
  - `html_helpers/{shortprops,diagonals}.rs`, `semigroup_cones/src/main.rs`
    (incl. JSON emission), `semigroup_explorer/src/js_eval.rs`,
    `tests/integration.rs`, `jsmodules/app.js`, `tex/writeup_updown.tex`.
Refreshed waldicone cached HTML/JSON for all gmax (2, 10, 11, 13, 14).

TODO 95) [DONE]
crates/semigroup_math/tests/integration.rs split into:
  tests/math_tests.rs        — gcd, binom_basics, binom_overflow (pure-math)
  tests/semigroup_tests.rs   — every Semigroup-touching test (renamed from
                               integration.rs via `git mv`)
Cargo treats every tests/*.rs as its own integration binary so the split
is free. README and CLAUDE.md architecture diagrams updated to match.

TODO 96) [DONE]
Took option (a) + the directory-split alternative discussed before
implementing; rejected the trait extraction because it adds `use`
ceremony at every call site and the `.e()` / `.e` ergonomics you
flagged in the original note. The public fields stay public.

Layout after the refactor:
  crates/semigroup_math/src/math/
    mod.rs                      — module wiring, gcd, gap_block, to_gap
    semigroup_compute.rs        — compute(): sliding-window builder
    semigroup/mod.rs            — Semigroup struct + PartialEq/Eq/PartialOrd
    semigroup/numerical.rs      — wilf, kunz, diag, main_diag, r_i, rho
    semigroup/vec_props.rs      — blob()
    semigroup/bool_props.rs     — element, is_gap, is_reflected_gap,
                                  is_descent, is_descent_image, v_in_s,
                                  classify

`pub use semigroup::Semigroup;` and `pub use semigroup_compute::compute;`
keep the existing `math::Semigroup` / `math::compute` import surface
unchanged for downstream crates. README architecture diagram updated.

TODO 96) [DONE]  (second; the ascent/descent rewrite)
Sequence of changes:
  - Drop the 5 di-asserting tests, doc-note `is_descent_image` as
    stale, then later remove `is_descent` / `is_descent_image`
    (column `di`) from the codebase entirely. Refreshed waldicone
    caches so no JSON carries the obsolete `descent` / `di` fields.
  - Ascent generalised step-by-step:
      (a) fold the f+m fallback into the same loop body
          (k·a ∈ V(S) ∪ {f+m}, match condition is unified);
      (b) walk deeper strata (μ, f) ∩ (f − (l+1)m, f − l m) for
          l = 0..level−1, scanning k ∈ 1..=level at each l;
      (c) port the same walk to JS as ascentTarget so the 3D
          run-ascent loop animates the right cube.
  - Descent's else-branch filter was missing the `x < f + m` clause,
    so descent always pushed f. Fixed: largest Apéry strictly in
    (f, f+m) → push x−m. With that, ascent's `.max()` is now a
    left inverse: a ∘ d = S across the hand-picked round-trip
    cases including ⟨10, 11, 13⟩.
  - Final cleanup pass: O(1) Apéry membership
    (apery_set[v % m] == v) in both Rust and JS; trimmed doc / test
    narration.
Regression tests added: test_ascent_f_plus_m_branch_concrete,
test_ascent_k_at_least_2_concrete, test_ascent_deeper_strata_concrete,
test_ascent_inverts_descent_v_of_s_branch.

TODO 98) [DONE]
Dropped `*.pdf` from `tex/.gitignore`; committed the current
tex/writeup_updown.pdf so the rendered version travels with the
source.

TODO 99) [DONE]
New `html_helpers::testcase_csv(sg)` returns one CSV row in the
fixture format (gens;e;f;m;g;c;sym;apery;pf;t); pf sorted, apery in
residue-class order. WASM shim `JsSemigroup::testcase_csv`. New
"Tests" tab in index.html with a readonly textarea + Copy button
that mirrors the .csv tab. Every history append stashes the row on
`tr.dataset.testcase`; `buildTestcases()` joins them when the tab
becomes active. A round-trip unit test (compute(&[6,9,20]) →
exact-match against the first fixture row) pins the format.
User has already used the tab to capture 51 new corner cases into
the CSV — fixtures committed in b15b09f.

TODO 100) [DONE]
Added gap_block_extras() in math/mod.rs (called after the existing
scalar + matrix blocks). Emits GAP assertions for:
  - algebraic identities r + σ = g, f + r + 2 = 2g + 1, t ≤ r + 1
  - es and the inequality es ≤ e
  - rl and rl < t (or rl = 0 when t = 0)
  - V(S) ⊆ S
  - IsAlmostSymmetric, with ra = r as a consequence when true
  - the r_i list and ρ (the latter requires m ≥ 3)
Verified end-to-end by running the emitted script through real GAP
against the numericalsgps package on a spread of fixtures
(m = 2..6, sym / asym / almost-sym, multi-generator) — all assertions
pass.

TODO 101) [DONE]
remove the strata.html and that part of the sourcecode that is
associated with it, but keep the part for the highlighting
and the checkbox in index.html

TODO 102) [DONE]
in waldicone we generate a summary..list.html and a json file
if everything in the list is also in the json,
remove the generation of summary
also remove the gmax 13,11,10,2 - we don't really need them
delete the old index 5,6,7 html in ./normaliz and move the index8.html to ./history

TODO 103) [DONE]
in index.html under Generators we have a constraint=0 textbox
add a second inputbox directly below with a >0 label
user will put an expression there, and now the random buttons
will check for the expr1=0 and expr2>0 until a semigroup is found
once this is done to tests and then remove the
"Sym", "PSym" "Asym" "Sym+f" "PSym+f" and "ASym+f" buttons
(they all can be replaced by r=0 r-1=1 t-1-r=0 and Q=2g Q=2g+1 ..)

TODO 104) [DONE]
In the LaTex Tab in the latex we say "S is symmetric or S is not symmetric"
replace this by symmetric, pseudosymmetric, almost symmetric if r=0, r=1, r=t-1 (and r>1) respectively)
also show the r-Vector, i.e. (r_1,r_2,...r_{m-1}) with the 0 at \mu highlighted
call it reflected gaps mod i

TODO 105) [DONE]
at the bottom of our diagonals tab, after a hline
show a table with 1 + 2(m-1) columns and a
first row <th>  "rn", r_1, . . . , r_{m-1}, n_1, ..., n_{m-1}
second row <td> the values from the current semigroup
where the r_i are as usual and the n_i = q_i -  r_i the number of normal gaps

below that a matrix RSym with entries +1 and -1 such that
RSym rn = 0 is equivalent with r_i=r_{mu-i} for all i and r_\mu = 0
so RSym_{*,j} = 0 for j>m-1
RSym_{1,1}=1 RSym_{1,mu-1 mod m}=-1
RSym_{2,2}=1 RSym_{1,mu-2 mod m}=-1
RSym_{i,i}=1 RSym_{1,mu-i mod m}=-1
...
RSym_{mu,mu}=1 and 0 everywhere else
...
should have (m-1) rows, i think we might get rid of the symmetry later
compute the matrixproduct RSym * "rv" and check it is zero

compute (1......1) "nv" and show it is == g

TODO 106) [DONE]
place the RSym stoff on top of the tab
and i think we should add one column at the right to RSym
always 0 - than add one row at the bottom
(1,1,1,1,..,1,-g)
This way RSym nv = 0 - right?

TODO 107) [DONE]
(2m-f)*(f-m) > 0
(3m-f)*(f-2m) > 0
can replace the Rnd2m Rnd3m buttons, right?
Remove these two buttons as well, clean up the code



















