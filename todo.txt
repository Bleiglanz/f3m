TODO

(items 1–73 archived in history/todo_1_73.txt)

TODO 74) [DONE]
add a new checkbox "Strata" (next to S,Kunz,Gaps,Class)
it is not checked at the beginning
theory of strata (with left={1,...\mu-1} and right={mu+1,..m-1}
for l = 0...level(S) - we have defined level as floor(f/m)
define M_l := lm + i for i in left
define N_l := lm + i for i in right
then M_l - lm is a subset of left
and  N_l - lm is a subset of right
the idea would be that if strata is checked we see
a thick black border around the sets M_l and N_l, i.e. on every level we
would see
   |------------|        |------------|
lm |            | mu+lm  |            |
   |------------|        |------------|
so for every element in the strata we need to check
which borders to draw, something linke
i=1 only left, bottom, top border
1<i<mu-1 only top and bottom border
mu-1 only right and top and bottom
and then the same for i in mu+1...m-1

TODO 75) [DONE] I have not given up on the idea of counting semigroups
before going back to todo 73-72-71 let's study the general case
create a second wasm-html file strata.html
reuse as much code as possible, css, js etc.
Name should be "strata exporerer"
here we abstract from the left-right as in todo 74, lets go general:

input is a usize N and a lmax:usize

a strata sequence for l=0...l_max is a sequence of subsets
M_0 = emptyset contained in M_1 contained in M_2 .... contained in M_lmax contained in {1,...,N}
the only condition is that they go up,

should be in a html table like in index html
columns are 1,...,N
rows are l=0,...,lmax (lmax on top, next one down is lmax - 1, down to 0)

One Button Rnd1 : it creates a random subset at level 1 (one above the bottom of the table)
and fills the sequence by random numbers
at each step add something or do nothing - sets can only get bigger

the <th> from 1...N is repeated at the bottom

TODO 76) [DONE] currently the col index i is in the cells - replace by the number lm+i

TODO 77) [DONE] there is a simple symmetry 1<->N, 2<->N-1, 3<->N-2
col i corrosponds to col N-i+1
this would be if our invisible frobenius would be just lmax + N + 1--but we don't have that here
if mouse over col i, highlight both columns i and N-i+1

TODO 78) [DONE] if in each cell in col i is the number lm+i, find
the minimum number w_i in each cell (it is the first time it appears while l goes up)
add a new row at the bottom
w_1,...w_N
and another one showing these minimus

TODO 79) [DONE] show missing - call the value val(l,i)=lm+i
the cell l,i represents the number ml+i
if i hover a empty cell: highlight the gaps (= empty cells)
that have the val(l,i)+w_j for all j

TODO 80) [DONE] in the 3d view only in the tab
add two buttons "run descent" and "run ascent"
clicking on them will toggle the description to "stop descent" resp. "stop ascent"
if one is running, user cannot click on the other one
"run ascent" is only available if is_descent is true, i.e. we can ascent at all
once running, it does at most 500 iterations of
compute the new semigroup descent or ascent of the current one (but don't change the view)
do an animation
update the 3dview with the new semigroup, but keep all viewport settings as the user has it
wait for 1second
the animation is this

descent of an apery w such that f < w < f+m
--animates the movement of the cube w to the w-m position
--lets the gap-cube w-m directly below it explode
--lower the cube to the w-m position one level down
--replace the semigroup with the new one
descent of f+m
--let f explode
--lower the f+m cube to the f position
--replace the semigroup with the new one
ascent of a minimal generator a with f-m<a<f
--let the a cube explode
--replace the view with the new semigroup (colors could change)
ascent of a minimal generator f+m
--let the f+m cube explode
--replace


TODO 81) [DONE]
directly below the random button show a small text
constraint = 0
and a textbox below (same width as the buttons)
in the box the user can enter a expr
exactly the same capabilites as our expr parser allows
if there is anything in this box:
if the user clicks on random, the constraint is given
to the create random as a string
random goes into its loop
creates a random semigroup
computes the value of the constraint expr with e,m,t,r,... etc
(if the expr throws an error - ignore)
if value of the expr == 0 then return the semigroup
otherwise iterate
(ignroe the problem that the expr==0 might be impossible - too hard to adapt the random parameters we use now)

TODO 82) [DONE]
added new props in semigroup.rs
    /// es = number of small minimal generators, the ones <f-m
    pub es: usize,

    /// rl = large reflected gaps, gaps L such that f-m < L < f (automatically reflected)
    pub rl: usize,

add them to the history, short_props, waldicone summary+json, Pivot
also reorder the history, shortprops
instead of
m	f	e	g	σ	r	ra	fg	t
use
m   f  es   e   σ   g  rl   t  r   ra  fg
in integration.rs check the tests
    // todo 82
    assert!(s.rl <= s.t);
    assert!(s.t <= s.r +1);
    assert!(s.es <= s.e);


TODO 83) [DONE]
combined_table now emits a top "sg-arrow-row" with one cell per residue
column:
- residue 0: ↓ → toggle(m)
- residue i ≠ 0 with w_i > f: ↓ → toggle(w_i)
- residue i ≠ 0 with f − m < w_i < f: ↑ → toggle(w_i)
- otherwise: empty
The arrow spans carry `data-n` so the existing `.sg-grid span[data-n]`
click handler routes the click through doToggle.
Visual check on ⟨6, 9, 20⟩: res 0 ↓6, res 1 ↓49, res 2 empty, res 3 empty,
res 4 ↑40, res 5 empty.

TODO 84) [DONE]
PROP_THEAD_TR adds a final `<th>Δt</th>` column; historyRow appends a
`<td>` showing `t(current) − t(previous)` (signed, "—" for index 0).
CSV header gets a matching `Δt` column. Help-tab description updated.
The Comp-tab shortprop (Rust) is unchanged because it always shows
prev/curr side-by-side.

TODO 85) [DONE]
  - Almost-symmetric invariant: already asserted at integration.rs:486
    (`assert_eq!(s.ra, s.r, "almost-symmetric ⇒ ra=r for {gens:?}")`).
    Audit item was a false alarm — no change needed.
  - rl ≤ t strictness: strengthened to `rl < t` whenever `t > 0`. Proof
    in the comment at check(): every gap counted by rl is pseudo-Frobenius
    and strictly below f, while f itself is always in PF and never in rl,
    so rl + 1 ≤ t.
  - Pivot tab drill-down field-shelf polish: deferred — vague item, no
    concrete pain point identified.
  - Waldicone HTML sort/filter: deferred — adding sort interactivity
    needs a JS lib (tablesort.js or similar). Users wanting sortable
    views have the JSON + Pivot tab.

TODO 86) [DONE] up-down = identity?
findings (commit 4e175f9 + the experiment in this commit):
- descent(ascent(S)) == S holds iff ascent enters its branch (2):
      f+m is a min-gen AND no min-gen g satisfies m < g < f and g+m > f
  the "large min-gen" branch (1) is NOT invertible by descent in general.
  proof sketch in the manipulators::ascent doc comment.
- counterexample: ⟨4, 5, 7⟩ -> ⟨4,7,9,10⟩ -> ⟨3, 4⟩ (test
  test_up_down_identity_fails_in_large_gen_branch in integration.rs).
- the gated identity is asserted in the main check() loop and as a
  dedicated test test_up_down_identity_fpm_branch in integration.rs.
- duality between Semigroup::ascent and Semigroup::descent is documented
  in their doc comments in manipulators.rs.

TODO 87) [DONE]
test_up_downs(s) reviewed and documented. Each branch is justified in the
doc comment on the function; the conditions are:
  - descent: w > f && f > m  (always: count_gap+1; legs gated by
    v_in_s vs w<f+m)
  - ascent : w ∈ gen_set && (w<f && w+m>f  OR  w == f+m)
All assertions hold across the full CSV fixture set + property tests
(integration suite: clean run, no failures).
Outer descent guard `f > m` skips f≤m (e.g. ⟨2,3⟩); descent `w == f+m`
with `!v_in_s()` only asserts `count_gap+1` (other invariants unconstrained
for that geometry).

TODO 88) [DONE]
arrow_cell in html_helpers/src/combined_table.rs now mirrors test_up_downs:
  - residue 0: never an arrow (test loop is 1..m)
  - ↓ iff `w > f && f > m`, data-n = w − m (the gap that descent adds)
  - ↑ iff `w ∈ gen_set` and either `f − m < w < f` or `w == f + m`
CSS: font-size 1.6em, font-weight 900, row height bumped to 2em.

TODO 89) [DONE]
Three buttons added in index.html under the existing Sym/PSym/ASym row:
  "Sym (r=0)+f", "PSym (r=1)+f", "ASym (r=t-1)+f".
driveCreator / driveRandomCreator now accept an optional postProcess(sg);
the +f buttons pass `addF = sg => sg.toggle(sg.f).gen_set`. Constraint
matching is applied to the *base* semigroup, before toggle(f). Help-tab
row updated.

TODO 90) [DONE]
tests/data/check_fixtures.csv holds 108 semigroup fixtures, format:
  gens;e;f;m;g;c;sym;apery;pf;t   (inner arrays space-separated).
One test (test_csv_fixtures) parses each row and calls check(); all
108 wrapper fns removed from integration.rs (file shrank ~2k lines).
On failure, the gens-bearing assertion messages in check() still
identify the offending row.























